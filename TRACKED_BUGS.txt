Sys_keys don't seem to work as intended.

Shader buffers, how can we fix them.

When minimizing the rended window, the app crashes

Event::AddListener: checker for if event has already subscribed sometimes crashes (dynamic cast is nullptr).

Changing camera's m_speed does not change its movement speed.